import { Polymer } from '@polymer/polymer/polymer-legacy'

if (!window.Polymer) {
    window.Polymer = Polymer
}

/**
 * style
 */
import './styles'

/**
 * pages
 */

import './pages'

/**
 * components
 */
import './components'
